import React from 'react';

export default class Signout extends React.Component {
  constructor(props) {
    super(props);
    this.state = {
    };
  }
  
  render() {
    return (
      <div>
        <h1>Signout page</h1>
      </div>
    );
  }
}